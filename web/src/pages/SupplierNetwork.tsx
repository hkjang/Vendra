import { useEffect, useMemo, useState } from 'react'
import { AlertTriangle, Network, Search, SlidersHorizontal, ZoomIn, ZoomOut } from 'lucide-react'
import { api, money } from '../api'
import { Badge, Loading, PageHeader, RiskBadge } from '../components'

type Node={id:string;name:string;riskLevel:string;grade?:string;annualSpend:number;categories:string[]}
type Edge={id:string;source:string;target:string;relationshipType:string;criticality:string;dependencyPercent?:number}
type NetworkData={nodes:Node[];edges:Edge[]}
export default function SupplierNetwork(){const [data,setData]=useState<NetworkData>();const [selected,setSelected]=useState<Node>();const [query,setQuery]=useState('');useEffect(()=>{api<NetworkData>('/api/v1/supplier-network').then(setData)},[]);const nodes=useMemo(()=>data?.nodes.filter(n=>!query||n.name.toLowerCase().includes(query.toLowerCase())).slice(0,24)||[],[data,query]);if(!data)return <Loading label="공급망 관계를 구성하는 중"/>;const positions=layout(nodes);const byId=new Map(positions.map(x=>[x.node.id,x]));return <div className="page network-page"><PageHeader eyebrow="Multi-tier supply network" title="공급망 Network" description="N차 협력업체 관계, 공급 집중도와 연쇄 리스크를 그래프로 분석합니다." actions={<button className="button secondary"><SlidersHorizontal/>Risk 전파 설정</button>}/><div className="network-toolbar"><div className="search-box"><Search/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="공급업체 검색"/></div><div><span><i className="node-low"/>LOW</span><span><i className="node-medium"/>MEDIUM</span><span><i className="node-high"/>HIGH · CRITICAL</span></div><button className="icon-button"><ZoomOut/></button><button className="icon-button"><ZoomIn/></button></div><div className="network-layout"><div className="network-canvas"><svg viewBox="0 0 900 610" role="img" aria-label="공급업체 관계 그래프"><defs><filter id="shadow"><feDropShadow dx="0" dy="4" stdDeviation="5" floodOpacity=".13"/></filter><marker id="arrow" markerWidth="7" markerHeight="7" refX="6" refY="3.5" orient="auto"><path d="M0 0L7 3.5L0 7Z" fill="#9aa4b5"/></marker></defs>{data.edges.map(e=>{const a=byId.get(e.source),b=byId.get(e.target);if(!a||!b)return null;return <g key={e.id}><line x1={a.x} y1={a.y} x2={b.x} y2={b.y} className={e.criticality==='critical'?'critical-edge':''} markerEnd="url(#arrow)"/><text x={(a.x+b.x)/2} y={(a.y+b.y)/2-5}>{e.dependencyPercent?`${e.dependencyPercent}%`:e.relationshipType}</text></g>})}{positions.map(({node,x,y},i)=><g className={`network-node ${node.riskLevel.toLowerCase()} ${selected?.id===node.id?'selected':''}`} key={node.id} onClick={()=>setSelected(node)} transform={`translate(${x},${y})`} tabIndex={0}><circle r={i<3?34:27} filter="url(#shadow)"/><text textAnchor="middle" y="-2">{node.name.slice(0,8)}</text><text className="node-grade" textAnchor="middle" y="13">{node.grade||node.riskLevel}</text></g>)}</svg>{!nodes.length&&<div className="network-empty"><Network/><b>표시할 공급업체가 없습니다</b></div>}<div className="network-hint">노드를 선택하면 공급업체의 위험과 의존도를 확인할 수 있습니다.</div></div><aside className="network-inspector">{selected?<><div className="network-company"><span className="company-avatar hero-avatar">{selected.name.slice(0,2)}</span><div><h2>{selected.name}</h2><p>{selected.categories.join(', ')||'품목 미지정'}</p></div></div><div className="network-risk"><span>Risk level</span><RiskBadge level={selected.riskLevel}/><strong>{selected.grade||'—'}</strong><small>Supplier grade</small></div><dl><dt>연간 구매금액</dt><dd>{money(selected.annualSpend)}</dd><dt>직접 연결</dt><dd>{data.edges.filter(e=>e.source===selected.id||e.target===selected.id).length}개</dd><dt>공급 품목</dt><dd>{selected.categories.length}개</dd></dl>{selected.riskLevel==='HIGH'||selected.riskLevel==='CRITICAL'?<div className="network-alert"><AlertTriangle/><span><b>연쇄 리스크 주의</b>연결된 상·하위 공급업체의 대체 가능성을 검토하세요.</span></div>:<div className="network-safe"><Badge tone="success">정상 모니터링</Badge></div>}<a className="button" href={`/suppliers/${selected.id}`}>Supplier 360 열기</a></>:<div className="inspector-empty"><Network/><h3>공급업체를 선택하세요</h3><p>평가, 위험, 구매금액과 연결 관계를 표시합니다.</p></div>}</aside></div></div>}
function layout(nodes:Node[]){const cx=450,cy=300;return nodes.map((node,i)=>{if(i===0)return{node,x:cx,y:cy};const ring=i<=8?1:2;const inRing=ring===1?8:Math.max(1,nodes.length-9);const idx=ring===1?i-1:i-9;const angle=idx/inRing*Math.PI*2-Math.PI/2;const radius=ring===1?145:255;return{node,x:cx+Math.cos(angle)*radius,y:cy+Math.sin(angle)*radius}})}
