# Changelog

## Unreleased

### Added

- 데이터가 있는 구버전 스키마의 업그레이드 통합 테스트(`VENDRA_TEST_UPGRADE_DSN`). v0.3.0 시점 스키마와 실데이터를 만들어 현재까지 올리고, 누적된 중복 승인 정리·기존 데이터 보존·신규 설정 시드·재실행 무해함을 확인합니다. 기존 마이그레이션 테스트는 빈 데이터베이스만 다뤘습니다
- 입력 자동 저장의 동시성 테스트. 상한에 걸린 상태에서 16건을 동시에 저장해도 각 저장이 살아남고 상한이 지켜지는지 확인합니다

## v0.6.20 — 2026-08-21

### Fixed

- **같은 문서를 동시에 올리면 버전 번호가 중복 부여되던 문제.** 버전을 `max(version)+1`로 계산하는데, 동시 요청이 같은 스냅샷을 읽어 같은 번호를 주장할 수 있었습니다. 12개 동시 업로드에서 한 번호가 최대 6개 파일에 붙는 것을 재현했습니다. 버전 이력이 어느 파일이 어느 버전인지 말할 수 없게 됩니다. 이제 해당 문서 이력에 트랜잭션 범위 잠금을 걸어 번호 부여를 직렬화합니다. 다른 문서의 업로드는 영향받지 않습니다

## v0.6.19 — 2026-08-21

### Fixed

- **동시에 상신하면 승인 요청이 여전히 중복 생성되던 문제.** v0.6.17에서 넣은 검사는 삽입 전에 읽기만 하므로, 동시 요청이 모두 그 검사를 통과할 수 있었습니다. 24개 동시 상신에서 승인이 2~11건 열리는 것을 재현했습니다. 이제 `workflow_instances`에 부분 유니크 인덱스를 걸어 데이터베이스가 보장하고, 경합에서 진 요청은 오류 대신 이미 진행 중인 승인을 돌려받습니다. 기존에 쌓인 중복은 마이그레이션이 가장 오래된 건만 남기고 `superseded`로 정리합니다

### Added

- 동시성 통합 테스트 2종 — 동시 상신이 승인을 하나만 여는지, 동시 승인이 한 번만 처리되고 나머지는 409를 받는지 확인합니다
- OIDC 로그인 흐름의 통합 테스트. 가짜 IdP(discovery·JWKS·토큰 엔드포인트)를 세워 PKCE, state, nonce, ID 토큰 서명 검증과 세션 발급까지 실제로 걸어봅니다. v0.6.1에서 막은 계정 탈취 경로 — 검증되지 않은 이메일로 기존 계정을 가로채는 시도 — 가 실제 흐름에서 거부되고, 검증된 뒤에는 정상 연결되며, 이미 연결된 subject는 이메일 재검증 없이 통과하는 것을 확인합니다

## v0.6.18 — 2026-08-21

### Fixed

- **문서를 승인 서명하면 그 문서를 더 이상 내려받을 수 없던 문제.** 승인 서명이 문서 상태를 `approved`로 바꾸는데, 다운로드·미리보기는 `status='active'`인 문서만 조회했습니다. 목록에는 계속 보이지만 열면 "문서를 찾을 수 없습니다"가 나오고, 같은 이유로 **두 번째 서명(공동 결재)도 불가능**했으며 **만료 예정 알림도 더 이상 발송되지 않았습니다.** 승인은 서명의 결과이지 문서의 종료 상태가 아니므로, 이제 `active`와 `approved`를 모두 사용 가능한 상태로 취급합니다

### Added

- 문서 수명주기 통합 테스트. 업로드·체크섬·승인 서명·상태 전이·다운로드와 한글 파일명 전달까지 실제 API로 검증합니다
- 보완 요청 후 재상신 통합 테스트. 반환된 건이 새 승인으로 1단계부터 다시 시작하는지 확인합니다

## v0.6.17 — 2026-08-21

### Fixed

- **같은 건을 다시 상신하면 승인 요청이 중복 생성되던 문제.** 상신 시 이미 진행 중인 승인이 있는지 확인하지 않아, 버튼을 두 번 누르거나 응답이 느려 재시도하면 승인 요청이 그만큼 열렸습니다. 각각 승인함에 별도로 표시되고, 하나를 승인하면 나머지는 이미 처리된 건에 대한 유령 요청으로 남았습니다. 이제 진행 중인 승인이 있으면 그것을 그대로 반환합니다

### Added

- 다단계 승인 흐름의 통합 테스트. 상신부터 단계별 역할 승인, 타 단계 역할의 승인 차단까지 실제 API로 검증합니다

## v0.6.16 — 2026-08-21

### Fixed

- **구매 원장의 조직이 검증되지 않아 지출이 남의 조직으로 집계되던 문제.** 조직별 Spend 분석은 `spend_transactions.organization_id`를 그룹 키로 쓰는데, 등록 시 이 값을 검증하지 않았습니다. 공급업체는 이미 검증하고 있었으므로 데이터가 범위를 넘지는 않았지만, 자기 공급업체의 지출을 접근할 수 없는 조직 앞으로 기록해 조직별 집계를 왜곡할 수 있었습니다

## v0.6.15 — 2026-08-21

### Fixed

- **다른 공급업체의 발주가 계약금액 초과 알림을 잘못 발생시키던 문제.** 발주 누계 집계가 `parent_id`만 보고 조인해 공급업체 일치를 확인하지 않았습니다. `parent_id`는 요청에서 그대로 받는 값이라, 무관한 발주 하나로 남의 계약에 **critical 등급 초과 알림**을 띄우고 잘못된 누계 금액을 본문에 담을 수 있었습니다. 이제 해당 계약의 공급업체 발주만 집계합니다

## v0.6.14 — 2026-08-21

### Security

- **포털 초대에 데이터 범위 검증이 없던 문제.** 초대는 향후 만들어질 포털 계정을 지정한 공급업체에 묶는데, 그 공급업체 ID를 검증하지 않았습니다. 부서 범위 사용자가 **자신이 볼 수 없는 공급업체의 포털 계정을 만들 수 있는 초대를 발급**할 수 있었습니다
- **소싱 참여업체 초대에 공급업체 범위 검증이 없던 문제.** RFQ 자체는 검증하지만 초대할 공급업체 ID는 검증하지 않아, 범위 밖 공급업체를 자신의 입찰에 끌어들이고 그 업체에 해당 입찰의 포털 열람 권한을 줄 수 있었습니다

## v0.6.13 — 2026-08-21

### Security

- **공급망 관계 등록에 데이터 범위 검증이 없던 문제.** 네트워크 그래프 조회는 간선의 양쪽 공급업체를 모두 조직 범위로 거르지만, 등록은 두 공급업체 ID를 검증 없이 받았습니다. 부서 범위 사용자가 **볼 수 없는 공급업체와의 공급 관계를 만들어 넣을 수 있었고**, 그 간선은 다른 조직의 그래프와 연쇄 리스크 분석에 나타나면서 정작 작성자에게는 보이지 않았습니다. 같은 파일의 지출 등록은 이미 같은 검증을 하고 있었습니다

## v0.6.12 — 2026-08-21

### Fixed

- **공급업체 검색에 빠르게 입력하면 목록이 비고 로딩 상태로 멈추던 문제.** 입력할 때마다 요청이 나가는데 응답 순서를 보장하지 않아, 먼저 보낸 요청이 나중에 도착하면 최신 결과를 덮어썼습니다. 그 결과 화면이 기다리는 키와 어긋나 목록이 빈 채로 스피너에 머물렀고, 검색어를 다시 바꾸기 전에는 복구되지 않았습니다. 업무 목록에는 이미 같은 보호 장치가 있었습니다

## v0.6.11 — 2026-08-21

### Fixed

- **목록이 상한에서 조용히 잘리던 문제.** 업무·공급업체·문서 조회가 기본 100~200건에서 멈추면서 잘렸다는 사실을 알리지 않아, 화면에 보이는 것이 전부인 것처럼 읽혔습니다. 응답의 `count`도 총계가 아니라 반환 건수였습니다. 이제 상한보다 한 건 더 조회해 잘림 여부를 판별하고 `truncated`로 알리며, 목록 화면에 범위를 좁히라는 안내를 표시합니다

## v0.6.10 — 2026-08-21

### Fixed

- **데이터 조회가 실패하면 화면이 영원히 로딩 상태로 남던 문제.** 18개 화면이 `if (!items) return <Loading/>` 형태로 상태를 기다리는데, 요청이 거부되면 그 상태가 계속 `undefined`라 스피너가 사라지지 않았습니다. 실패 토스트는 몇 초 뒤 닫히므로 결국 설명 없는 스피너만 남고, 수동 새로고침 외에는 벗어날 방법이 없었습니다. 이제 대기가 비정상적으로 길어지면 원인 안내와 다시 시도 버튼을 함께 표시합니다. 정상 로딩에서는 나타나지 않습니다

## v0.6.9 — 2026-08-21

### Accessibility

- 공급업체·업무 목록의 검색창과 상태·리스크 필터에 접근 가능한 이름이 없어, 화면 낭독기 사용자가 무엇을 거르는 컨트롤인지 알 수 없던 문제. 정렬 필터에는 이미 이름이 있어 같은 줄에서 일부만 누락된 상태였습니다
- 대시보드 카드의 "전체 보기", "승인함" 링크가 18~19px로 최소 포인터 대상(24×24)에 미달하던 문제

## v0.6.8 — 2026-08-21

### Security

- **업무 객체 생성·수정에 쓰기 측 데이터 범위 검증이 없던 문제.** 조회는 항상 범위로 걸러졌지만 생성은 클라이언트가 보낸 `organizationId`와 `supplierId`를 그대로 받았습니다. 부서 범위 사용자가 **자신이 볼 수 없는 조직에 발주나 계약을 만들어 넣을 수 있었고**, 그 레코드는 해당 조직의 목록·대시보드·승인 라우팅에 들어가면서 정작 작성자에게는 보이지 않았습니다. 문서 업로드는 이미 같은 검증을 하고 있었습니다. 이제 생성 시 조직·공급업체를, 수정 시 공급업체 변경을 검증합니다

## v0.6.7 — 2026-08-21

### Security

- **제출하지 않은 입찰 초안이 구매자 비교표에 노출되던 문제.** 공급업체가 제출 없이 저장하면 `status='draft'` 행에 작성 중인 견적금액과 품목이 남는데, 비교표 조회에 상태 필터가 없어 그대로 표시됐습니다. 공급업체가 확정하지 않은 가격이 구매자에게 전달되는 것으로, 봉인 입찰에서 가장 피해야 할 상황입니다. 점수 계산은 이미 제출 건만 대상으로 했기에 초안은 금액만 있고 점수는 비어 있었습니다. 이제 제출 완료된 응답만 비교합니다

## v0.6.6 — 2026-08-21

### Security

- **공급업체 계좌번호 평문이 감사로그에 남던 문제.** 계좌는 AES-256-GCM으로 암호화해 저장하지만, 등록·수정 시 요청 본문을 그대로 감사 기록에 넣어 평문이 `audit_logs.new_value`에 남았습니다. `audit.read` 권한만 있으면 조회할 수 있어 암호화가 무력화됐습니다. 이제 감사에는 변경 여부(`bankAccountChanged`)만 남기고 값은 제외합니다

## v0.6.5 — 2026-08-21

### Fixed

- 업무 상태 일괄 변경에서 형식이 잘못된 알림 키 하나가 **배치 전체를 되돌리던 문제**. 키 패턴은 임의 문자열을 허용하는데 알림 ID는 uuid라, 캐스트 실패가 트랜잭션을 무효화하고 다른 업무의 상태 변경까지 사라졌습니다. 이제 알림 ID가 될 수 없는 키는 건너뜁니다

### Improved

- 사용자별 임시저장 개수를 50개로 제한하고 오래된 것부터 정리합니다. 임시저장 키는 클라이언트가 정하므로 종전에는 한 계정이 무제한으로 늘릴 수 있었습니다
- `maintenance.retention`에 `formDraftDays`(기본 60일) 추가. 제출되지 않고 방치된 임시저장을 정리합니다

## v0.6.4 — 2026-08-21

### Fixed

- **합격 기준이 없는 심사 템플릿이 모든 공급업체를 자동 합격시키던 문제.** `result_rules`가 비어 있으면 임계값이 전부 0이 되어 `total >= passMin`이 어떤 점수에도 참이 되고, 심사 결과가 `PASS`로 기록되며 공급업체 상태가 `approved`로 바뀌었습니다. 점수와 무관하게 통과하므로 심사가 형식만 남습니다. 이제 판정 불가능한 기준은 자동 합격 대신 `REVIEW_REQUIRED`로 사람의 판단을 요구하고 경고를 남깁니다
- 심사 템플릿 생성 시 `passMin > conditionalMin > reviewMin >= 0` 순서를 검증합니다. 종전에는 이름과 심사항목만 확인해 판정 불가능한 템플릿이 그대로 저장됐습니다

## v0.6.3 — 2026-08-21

### Fixed

- **통합 검색이 권한 범위 안의 결과를 조용히 누락하던 문제.** 업무 객체 조회가 `LIMIT 30`으로 자른 뒤에 권한을 검사해, 특정 유형만 볼 수 있는 사용자는 다른 유형이 상위를 차지하면 자기 결과를 잃었습니다. 계약만 조회 가능한 검토자가 더 최신인 이슈 40건에 밀려 계약 **3건 중 0건**을 받는 것을 재현했습니다. 이제 허용된 유형을 질의에서 걸러냅니다
- 통합 검색이 공급업체 포털 계정을 거부합니다. 검색은 전 공급업체를 조직 범위로 좁히는 방식이라 포털 격리를 담고 있지 않습니다
- 통합 검색의 업무 객체 조회 오류가 무시되어 부분 결과가 정상처럼 반환되던 문제

## v0.6.2 — 2026-08-21

### Accessibility

- **모달이 `aria-modal="true"`를 선언하면서 포커스를 가두지 않던 문제.** Tab이 뒤쪽 화면으로 빠져나가 키보드 사용자가 보이지 않는 컨트롤을 조작할 수 있었습니다. 정·역방향 모두 순환하도록 격리했습니다
- **모달을 닫으면 포커스가 `<body>`로 떨어지던 문제.** 복원 대상을 `useEffect`에서 읽는데 그 시점에는 `autoFocus` 필드가 이미 포커스를 가져간 뒤라, 모달 내부 입력란을 저장했다가 사라진 노드에 복원을 시도했습니다. 이제 렌더 시점에 여는 컨트롤을 기록하고 DOM 제거 이후 복원합니다
- **서비스 관리의 사용자·역할 표를 마우스로만 열 수 있던 문제.** 행에 키보드로 도달할 수 있는 핸들을 넣어 Enter·Space로 편집을 엽니다
- 12~13px 본문 텍스트 명암비를 WCAG AA(4.5:1) 기준으로 조정. 측정된 미달 색상 8종(최저 2.86:1)을 각 배경에 맞춰 최소폭으로 보정했습니다

### Improved

- **휴대폰에서 서비스 관리 화면이 가로로 밀리던 문제.** 관리자 내비게이션이 그리드 기본값(`min-width:auto`)으로 버튼 전체 너비만큼 늘어나고 표에는 스크롤 컨테이너가 없어 페이지가 217px 밀렸습니다. 내비게이션과 표가 각자 스크롤합니다
- 모바일 메뉴 열기·닫기 버튼이 31×21px로 최소 터치 영역(24×24)에 못 미치던 문제

## v0.6.1 — 2026-08-21

### Security

- **OIDC 계정 탈취 경로 차단.** `email_verified` 클레임을 구조체에 선언만 하고 전혀 검사하지 않은 채, `WHERE oidc_subject=$1 OR email=$2`로 이메일이 일치하는 기존 계정에 로그인시켰습니다. 사용자가 이메일을 임의로 설정할 수 있는 공급자에서는 관리자 이메일로 가입해 SSO 로그인하면 해당 계정을 그대로 획득할 수 있었습니다. 자동 생성 경로의 `ON CONFLICT(email) DO UPDATE SET oidc_subject=...`도 같은 방식으로 기존 계정을 덮어썼습니다. 이제 이메일 클레임에 의존하는 경로(기존 계정 연결·신규 생성)에만 검증을 요구하고, 이미 연결된 subject는 그대로 통과합니다
- **경쟁 입찰에서 참여업체가 노출되던 문제.** 소싱 Q&A의 `participants` 공개 질문에 질문자 업체명(`supplierName`)과 담당자명이 그대로 붙어, 입찰자가 답변을 읽으면서 **누가 함께 초대됐는지** 알 수 있었습니다. 이제 포털 조회에서는 본인 질문에만 출처가 남고, 구매팀 공지는 그대로 표시되며, 내부 검토자는 종전대로 전체 출처를 봅니다
- `oidc.requireVerifiedEmail` 설정 추가(기본 켜짐). 해당 클레임을 보내지 않는 공급자에 한해 관리자 화면에서 해제할 수 있으며, 거부는 `oidc_unverified_email` 감사 이벤트로 기록됩니다

## v0.6.0 — 2026-08-21

### Added

- 관리자 사용자 목록의 이름·이메일 검색과 조회 상한. 결과가 잘린 경우 목록에 명시해 전체를 본 것으로 오해하지 않게 합니다

### Improved

- `admin/users`, `admin/roles`, `admin/organizations`, `admin/access-grants`, `workflows` 조회에 상한 적용. 사용자가 많은 배포에서 전체를 한 응답에 직렬화하던 문제

### Security

- MCP 도구가 공급업체 포털 계정을 거부합니다. MCP 조회는 전부 조직 범위 기반이라 포털 격리를 담고 있지 않은데, REST와 달리 사용자 유형 분기가 없었습니다. 기본 역할로는 권한 게이트에 막히지만 관리자가 조회 권한을 부여하면 타사 공급업체 목록이 그대로 노출됐습니다
- MCP 오류 응답에서 드라이버 오류·캐스트 실패 등 내부 오류 문구를 제거하고 로그로만 남깁니다. AI Agent가 그대로 복창할 수 있는 경로였습니다

## v0.5.0 — 2026-08-21

### Added

- 모든 화면에서 동작하는 알림(Toast) 계층. 공급업체 포털에는 성공·실패 피드백 수단 자체가 없었습니다
- 처리되지 않은 요청 실패를 자동으로 알리는 전역 안전망. 세션 만료(401)는 로그인 화면 전환으로 이미 처리되므로 제외합니다
- 자기결재 차단 `workflow.separation_of_duties`. 본인이 요청한 건의 승인·반려를 금지하며(보완 요청은 허용), 기존 배포가 멈추지 않도록 기본값은 꺼짐입니다
- 활성 세션 화면: 기기별 IP·최근 활동·로그인 시각 확인, 개별 종료와 "다른 세션 모두 종료"
- `GET /api/v1/me/sessions`, `DELETE /api/v1/me/sessions/{id}`, `POST /api/v1/me/sessions/revoke-others`

### Improved

- 실패한 동작이 아무 표시 없이 사라지던 문제 전반. 승인 결정은 모달에 사유를 표시하고, 일괄 승인은 실패 건수 대신 실제 실패 사유를 보여줍니다
- 발주·계약 확인, 참여 거절, 평가위원 지정, 업체 초대, 질문 등록에 진행 중 표시와 이중 제출 방지
- 동일한 오류가 여러 번 겹칠 때 토스트를 중복 표시하지 않고, 오류는 12초 동안 유지해 읽을 시간을 줍니다
- 세션 목록의 시각을 "3시간 전 · 2026. 8. 21. 오후 2:10" 형태로 표시하는 `dateTime`·`timeAgo` 추가
- 로그인·가입 화면의 버전·SSO 조회는 화면 장식이므로 실패해도 경고를 띄우지 않습니다

### Fixed

- **관리자가 워크플로를 수정하면 진행 중인 승인이 영구히 막히던 문제.** 승인 단계는 상신 시점에 `workflow_instances.context`로 스냅샷되고 있었지만 승인 처리와 승인함이 이를 무시하고 최신 정의를 읽었습니다. 단계를 줄이면 진행 중 건이 목록 범위를 벗어나 `409`가 되어 승인·반려·보완요청 어느 것도 불가능해졌고, 순서를 바꾸면 엉뚱한 역할이 결재했습니다. 이제 스냅샷을 따릅니다
- 승인 처리 중 DB 오류가 "이미 처리된 승인입니다"(409)로 잘못 보고되던 문제. 존재하지 않는 건은 404, 실제 오류는 500과 로그로 구분합니다
- 한글 문서 파일명이 다운로드에서 깨지던 문제. `Content-Disposition`이 공백만 이스케이프해 RFC 8187 ext-value로 유효하지 않았고, 파서가 파라미터 전체를 거부해 파일이 URL 마지막 경로인 `download`로 저장될 수 있었습니다. `%`, `;`, `"`가 포함된 파일명도 같은 이유로 잘리거나 깨졌습니다. 이제 전체를 percent-encoding하고 ASCII `filename` 대체값을 함께 보냅니다
- 문서 응답의 `Content-Length`를 DB 기록값 대신 실제 파일 크기로 계산. 둘이 어긋나면 전송이 잘리거나 멈췄습니다

## v0.4.0 — 2026-08-21

### Added

- 로그인 무차별 대입 방어: 계정과 발신 IP 각각에 실패 임계값·잠금을 적용하고 `429`와 `Retry-After`를 반환하는 `security.login` 설정
- 로그인 실패(`login_failed`)와 잠금(`login_locked`) 감사 이벤트, `vendra_login_failures_total`·`vendra_login_lockouts_total` 지표
- 만료 세션과 오래된 로그인 시도 이력을 시간당 정리하는 `maintenance.retention` 보존 정책
- 비밀번호 변경 `POST /api/v1/me/password`: 현재 비밀번호 확인 후 교체하고 요청한 세션을 제외한 모든 세션 폐기
- 관리자 비밀번호 재설정 `POST /api/v1/admin/users/{id}/password`: 대상 사용자의 모든 세션을 즉시 폐기하는 자격증명 유출 대응 경로
- 최소 길이와 문자 종류를 정하는 `security.password` 정책. 사용자 생성, 포털 가입, 변경, 재설정에 공통 적용
- 개인화 화면의 비밀번호 변경 폼과 서비스 관리 사용자 편집의 재설정 폼

### Improved

- 라우트 단위 코드 분할로 첫 진입 번들 476kB → 271kB(gzip 134kB → 87kB). 공급업체 포털 사용자는 관리자·소싱 화면을 더 이상 내려받지 않습니다
- 화면 청크 로딩 실패 시 흰 화면 대신 새로고침 안내를 표시하는 오류 경계
- 알림 어댑터의 `timeoutSeconds` 설정과 통합 호출 전용 HTTP 클라이언트(연결 풀, 상한 타임아웃)
- 여러 인스턴스가 동시에 기동해도 스키마 마이그레이션이 경쟁하지 않도록 PostgreSQL advisory lock 적용
- 재현 가능한 오프라인 빌드를 위해 web 의존성을 `latest`에서 lockfile 버전으로 고정
- `make test`와 개발 문서의 Go 명령이 `web/node_modules`를 컴파일하지 않도록 대상 패키지 지정
- `VENDRA_TEST_DSN`, `VENDRA_TEST_MIGRATE_DSN`으로 실행하는 PostgreSQL 통합 테스트 추가 (미설정 시 자동 skip)

### Fixed

- IPv6 클라이언트에서 세션 IP가 잘못 기록되던 `RemoteAddr` 파싱 오류
- 계정이 없을 때 bcrypt 비교를 건너뛰어 응답 시간으로 계정 존재 여부가 노출되던 문제
- 포트 없는 피어 주소가 감사로그의 `inet` 변환을 실패시킬 수 있던 문제
- 비밀번호 최소 길이를 바이트로 세어 한글 4자(12바이트)가 "10자 이상"을 통과하던 문제
- 24자를 넘는 한글 `BOOTSTRAP_ADMIN_PASSWORD`가 bcrypt 72바이트 한도를 넘겨 서비스가 기동하지 못하던 문제. 이제 원인을 설명하는 오류로 즉시 거부합니다
- 관리자 사용자 생성에 비밀번호 길이 검증이 전혀 없던 문제
- 응답하지 않는 알림 웹훅이 `http.DefaultClient`의 무제한 대기로 배경 작업을 영구 정지시켜 알림과 정리 작업이 조용히 멈추던 문제
- 삭제된 화면 청크 요청에 `index.html`을 200으로 응답해 업그레이드 직후 브라우저가 MIME 오류로 실패하던 문제. 이제 404를 반환합니다

## v0.3.0 — 2026-08-21

### Added

- “업무 관제탑” 통합 인박스: 승인, 기한 업무, 계약 종료, 리스크 검토, 문서 만료와 미확인 알림 집계
- 신호등·레이더·시간 레인으로 긴급도와 처리 순서를 표현하는 관제탑 시각 메타포
- 개인 필터·정렬·표시 열 보기 저장과 같은 조직 사용자 공유
- 신규 업무 입력의 사용자별 서버 자동 저장과 작성 내용 복원
- 업무 신호 일괄 확인·보류와 승인 요청 일괄 제출

### Improved

- 승인함 다중 선택, 일괄 승인·반려·보완 요청과 공통 의견 입력
- 관제탑 및 승인 상세의 데스크톱·모바일 반응형 구성
- 저장된 보기 적용·삭제와 현재 필터 미리보기
- 기한이 변경된 업무가 새로운 신호로 다시 나타나는 업무 상태 수명주기

### Fixed

- 계약 입력 화면의 자동 갱신 체크박스가 일반 입력 크기로 늘어나던 문제
- 자동 저장 삭제 실패가 정상 생성 결과를 실패로 오인하게 만들 수 있던 흐름
- 업무 링크의 공백과 특수문자가 검색 조건을 깨뜨릴 수 있던 문제

## v0.2.0 — 2026-08-13

### Added

- `Ctrl/⌘ K` 빠른 이동, 최근 이동 기록과 관리자 하위 메뉴 검색
- 관리자 서버 로그 화면: 검색, 레벨 필터, 자동 갱신, JSON 내보내기
- 구조화 로그의 최근 3,000건 메모리 보관과 민감 속성 자동 마스킹
- 알림 센터 새로고침, 전체 읽음 처리와 60초 주기 자동 갱신
- 공급망 그래프 확대·축소와 키보드 노드 선택
- 공급업체 포털 모바일 탐색, 도움말과 URL 해시 기반 메뉴 이동

### Improved

- 본문 14px, 보조 텍스트 최소 12px의 공통 가독성 기준 적용
- 주 메뉴, 관리자 메뉴, 포털, 모달, 승인 상세와 우측 패널 스크롤 개선
- 업무 기준의 한국어 메뉴 그룹, 메뉴 접기 상태 저장과 현재 메뉴 자동 노출
- 모바일 입력 확대 방지, 포커스 표시, 본문 바로가기와 모달 포커스 복원
- 공급업체 심사, 소싱 평가·선정·답변과 참여 거절 폼의 검증 UX
- 알림, 로그, 빠른 이동 화면의 데스크톱·모바일 반응형 디자인

### Fixed

- PostgreSQL의 `+00` 시간대 문자열 때문에 일부 목록이 빈 화면이 되던 오류
- 심사 의견에 저장된 값이 있을 때 입력을 완전히 지울 수 없던 오류
- 긴 메뉴와 상세 우측 패널에서 내용이 잘리던 문제
- 모바일에서 데스크톱의 접힌 메뉴 상태가 그대로 적용되던 문제
- 동작하지 않는 장식성 버튼과 브라우저 기본 `prompt` 의존 입력 흐름
